import React from 'react';

var World = React.createClass({
    render(){
        console.log('world render');
        return (
            <div>Hello React...</div>
        );
    }
});

export default World;
